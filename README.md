# Friend Chat Web (Flutter)

A starter Flutter web app for chatting with your friend.

## Run locally

1. Install Flutter SDK.
2. Enable web support:
   ```bash
   flutter config --enable-web
   ```
3. Get dependencies:
   ```bash
   flutter pub get
   ```
4. Run in browser:
   ```bash
   flutter run -d chrome
   ```
